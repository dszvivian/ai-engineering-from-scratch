## Matrices:

Tells us how much it strectched or scaled in the space compared to unit matrix (identitiy Matrix).
It also tells about the directions.

It can also be called finding area.

det = 2 → area doubles
det = 0 → flattened (no area)
det = −2 → area doubles + flipped